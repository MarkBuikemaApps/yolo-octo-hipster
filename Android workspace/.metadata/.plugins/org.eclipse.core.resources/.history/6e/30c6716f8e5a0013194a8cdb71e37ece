package com.markbuikema.juliana32.ui;

import android.view.View;
import android.view.animation.Animation;
import android.view.animation.Transformation;

import com.markbuikema.juliana32.R;
import com.markbuikema.juliana32.model.FacebookNieuwsItem;
import com.markbuikema.juliana32.model.NieuwsItem;
import com.markbuikema.juliana32.model.NormalNieuwsItem;

public class NieuwsDetailAnimation extends Animation {

	private View view;
	private NieuwsItem item;
	private int targetHeight;

	public NieuwsDetailAnimation(View view, NieuwsItem item) {
		this.view = view;
		this.item = item;

		if (item instanceof NormalNieuwsItem)
			TextView tv = view.findViewById(R.id.nieuwsitem_subtitle);
			else
			if (item instanceof FacebookNieuwsItem)
				if (item.isPhoto()) {

				} else {

				}		
	}

	@Override
	protected void applyTransformation(float interpolatedTime, Transformation t) {

	}

	@Override
	public boolean willChangeBounds() {
		return true;
	}
}
